print "Capitalize a String Exercise"
# prompt user for string and return the user's string capitalized

user_string = raw_input("Enter a word or sentence: ")

print user_string[0].upper() + user_string[1:]
